import XCTest
@testable import LocStringCleaner

final class LocStringCleanerTests: XCTestCase {
    func testExample() throws {
        XCTAssertEqual(LocStringCleaner().text, "Hello, World!")
    }
}
